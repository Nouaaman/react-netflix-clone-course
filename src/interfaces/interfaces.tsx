export interface MovieIterface {
    id: number,
    title: string,
    overview: string,
    release_date: Date,
    poster_path: string,
    backdrop_path: string
}

export interface TvIterface {
    id: number,
    name: string,
    overview: string,
    release_date: Date,
    poster_path: string,
    backdrop_path: string
}